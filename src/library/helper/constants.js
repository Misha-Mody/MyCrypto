const BASE = "https://api.coingecko.com/api/";

const HOST = "api.coingecko.com";

const API_VERSION = "3";

const URI = `${BASE}v${API_VERSION}`;

export default {
  BASE,
  HOST,
  API_VERSION,
  URI,
};
